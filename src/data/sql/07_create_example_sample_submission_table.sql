DROP TABLE IF EXISTS example_sample_submission;

-- Create a table for the example_sample_submission data
CREATE TABLE example_sample_submission
(
    row_id INTEGER,
    answered_correctly INTEGER,
    group_num INTEGER
);
